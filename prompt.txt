I want to redesign the user-facing dashboard/site with a clean, 
professional style. All cards, topbar, and bottom navbar must have 
opacity 100% (no transparency/blur). 

The color theme should be a rich, deep navy blue + gold accent combination 
(professional finance app style).

Please do the following:

=== STEP 1: Find all relevant files ===
1. List all files in templates/ folder
2. Find the main user dashboard template (dashboard.html or home.html)
3. Find the main CSS file (static/css/ folder) — show me its contents
4. Check if there is a base.html or layout template

=== STEP 2: Fix opacity on ALL elements ===
Search for any of these in CSS and templates and set them to opacity: 1 or remove transparency:
- opacity: 0.X  → change to opacity: 1
- rgba(X, X, X, 0.X)  → change alpha to 1  OR replace with solid color
- background: transparent → replace with solid color
- backdrop-filter → remove it
- -webkit-backdrop-filter → remove it

=== STEP 3: Apply this exact color system to ALL cards, topbar, bottom navbar ===

Use this color palette:
  --color-primary:     #0A1628   /* deep navy */
  --color-secondary:   #112240   /* card background */
  --color-accent:      #C9A84C   /* gold */
  --color-accent-light:#F0C96B   /* light gold */
  --color-surface:     #1A2F4E   /* elevated card */
  --color-border:      #1E3A5F   /* card border */
  --color-text:        #FFFFFF   /* white text */
  --color-text-muted:  #8899AA   /* muted text */
  --color-success:     #00C853   /* green for ACTIVE badge */
  --color-danger:      #FF1744   /* red */

Apply these styles:

/* Topbar */
.topbar, header, nav.top {
  background: #0A1628 !important;
  opacity: 1 !important;
  backdrop-filter: none !important;
}

/* Bottom Navbar */
.bottom-nav, .navbar-bottom, nav.bottom {
  background: #0A1628 !important;
  opacity: 1 !important;
  border-top: 1px solid #C9A84C !important;
  backdrop-filter: none !important;
}

/* All Cards */
.card, [class*="card"], .widget, .balance-card, .service-card, .currency-card {
  background: #112240 !important;
  border: 1px solid #1E3A5F !important;
  border-radius: 16px !important;
  opacity: 1 !important;
  backdrop-filter: none !important;
  box-shadow: 0 4px 20px rgba(0,0,0,0.4) !important;
}

/* Gold accent for buttons and highlights */
.btn-primary, .quick-loan-btn, [class*="btn-main"] {
  background: linear-gradient(135deg, #C9A84C, #F0C96B) !important;
  color: #0A1628 !important;
  font-weight: bold !important;
  border: none !important;
  opacity: 1 !important;
}

/* ACTIVE badge */
.badge-active, .status-active, .badge.active {
  background: #00C853 !important;
  color: white !important;
  opacity: 1 !important;
}

/* Page background */
body, .main-content, .page-wrapper {
  background: #0A1628 !important;
  opacity: 1 !important;
}

/* Section titles like "OUR SERVICES", "CURRENCY" */
.section-title, h2, h3 {
  color: #C9A84C !important;
}

/* Currency table */
table, .currency-table {
  background: #112240 !important;
  border-radius: 12px !important;
}
table th {
  color: #C9A84C !important;
  background: #1A2F4E !important;
}
table td {
  color: #FFFFFF !important;
  border-color: #1E3A5F !important;
}

=== STEP 4: Apply to all user-facing templates ===
Apply changes to:
- dashboard.html (or home.html)
- base.html or layout.html (so it applies globally)
- Any inline styles in templates — replace with the new colors

=== STEP 5: Do NOT touch staff/admin templates ===
Only change user-facing templates and CSS. 
Do not modify anything in staff_*.html files.

Show me which files you are changing, then apply all changes.