Remove dark background overlays from ALL pages to match choose.html brightness:

PROBLEM: 
- choose.html has bright, visible background (GOOD - keep this)
- login.html has dark/black overlay making background too dark (BAD - fix this)
- Other pages may also have dark overlays

GOAL:
Make ALL pages have the same bright background as choose.html

FILES TO CHECK AND FIX:
1. login.html
2. register.html  
3. Any other template files with dark overlays

WHAT TO REMOVE:
Find and remove ALL of these dark overlay patterns:

1. Body/HTML dark overlays:
   body { background: linear-gradient(rgba(0,0,0,0.X), ...) }
   Remove the rgba(0,0,0,...) gradient layer
   Keep only the background image

2. Dark wrapper divs:
   Any div with:
   - background: rgba(0,0,0,...)
   - background: linear-gradient with rgba(0,0,0,...)
   That covers the whole page

3. Backdrop filters causing darkness:
   - backdrop-filter: brightness(...) if < 1
   - Any filter making things darker

WHAT TO KEEP:
- Card backgrounds (the form cards with blue/dark styling)
- Button backgrounds
- Input field backgrounds
- All functionality and structure
- Django template tags {% %}

REFERENCE (GOOD):
choose.html body background - bright and visible
Copy this background style to other pages

REFERENCE (BAD):  
login.html - has dark overlay, background too dark
Remove the dark overlay layer

IMPORTANT:
- Only modify CSS background/overlay properties
- Do NOT change card styling
- Do NOT change Django template structure
- Do NOT change form functionality
- Make background consistent across ALL pages