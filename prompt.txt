Update profile.html and loan_info.html for Information viewing feature:

CHANGES TO MAKE:

1. PROFILE.HTML - Change Button Text and Link:
   Find the "Loan Application" menu button
   
   Current:
   - Title: "Loan Application"
   - Subtitle: "Apply & track your status"
   - Link: {% url 'quick_loan' %}
   
   Change to:
   - Title: "Information"
   - Subtitle: "View your application details"
   - Link: {% url 'loan_info' %}
   
   Keep same icon and styling

2. LOAN_INFO.HTML - Add View-Only Mode:
   
   A. Check if user already confirmed application:
      - If application status is APPROVED or REJECTED or PENDING
      - OR if user has already submitted loan application
      - Then activate VIEW-ONLY mode
   
   B. In VIEW-ONLY mode:
      - All input fields become DISABLED (cannot edit)
      - Show GREEN checkmark on completed steps
      - Hide "Confirm Application" button OR show as disabled
      - Add message: "Application submitted - View only"
      - Keep all data visible but not editable
   
   C. Green checkmark for completed steps:
      CSS for completed step icon:
      - Background: #22c55e (green)
      - White checkmark icon
      - Or add "✓" symbol next to step title
   
   D. Disable all inputs in view-only mode:
      Add disabled attribute to:
      - All <input> fields
      - All <select> dropdowns
      - All <textarea> fields
      - File upload buttons
      - Bank selection
   
   E. Visual indicator:
      Add banner at top of page in view-only mode:
      "Your application has been submitted. Information shown below is for reference only."

3. WHAT TO KEEP UNCHANGED:
   - Django template structure
   - URL routing (backend handles this)
   - Form field names
   - Validation logic
   - Other profile menu items
   - Page styling and colors

IMPLEMENTATION NOTES:
- Use Django template conditional: {% if user.has_submitted_application %}
- Or check loan status: {% if loan_status in "APPROVED,REJECTED,PENDING" %}
- Add CSS class "view-only" to form when in view mode
- Use :disabled pseudo-class for styling disabled inputs

Example disabled input:
<input type="text" name="full_name" value="{{ user.full_name }}" disabled readonly>

Example completed step indicator:
<div class="step-icon completed">
  <svg><!-- checkmark icon --></svg>
</div>

.step-icon.completed {
  background: #22c55e !important;
  border-color: #22c55e !important;
}